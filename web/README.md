# SmartTermin Web

This package contains the React + Vite web client for SmartTermin.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Environment

Copy [`./.env.example`](./.env.example) to `./.env.local` and provide your own values before running the app.
